name: Tatneft Trading Bot

on:
  schedule:
    # Запуск каждый день в 6:40 по Москве (3:40 UTC)
    # Moscow time = UTC+3, so 6:40 MSK = 3:40 UTC
    - cron: '40 3 * * *'
  
  # Ручной запуск (для тестирования)
  workflow_dispatch:

jobs:
  send-report:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Клонирование репозитория
        uses: actions/checkout@v3
        
      - name: 🐍 Настройка Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: 📦 Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install requests pytz
          
      - name: 🤖 Запуск бота Татнефть
        env:
          # ВНИМАНИЕ: Имена переменных должны совпадать с кодом!
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}  # ✅ Правильно
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}  # ✅ Правильно
        run: python tatneft_bot.py
        
      - name: ✅ Проверка успешности
        run: echo "✅ Бот успешно выполнен в $(date '+%H:%M') UTC"
